configuration:  
  projectName: seeu-eureka

server:
  port: 8000

management:
  security:
    enabled: false

eureka:
  instance:
    hostname: travelci.cloudapp.net
  client:
    registerWithEureka: false
    fetchRegistry: false