configuration:  
  projectName: seeu-eureka

server:
  port: 8000

management:
  security:
    enabled: false

eureka:
  instance:
    hostname: seeu-eureka
  client:
    registerWithEureka: false
    fetchRegistry: false